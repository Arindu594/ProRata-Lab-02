public class CircularFence {
    public static void main(String[] args) {

        double side = 10;
        double pi = 3.14;

        double ropeLength = 4 * side;

        double radius = ropeLength / (2 * pi);

        System.out.printf("Radius of the circular fence = %.2f", radius);
    }
}